# Port yang digunakan oleh shipping-service (Express.js akan listen di port ini)
PORT=3002

# URL koneksi ke RabbitMQ (rabbitmq adalah nama container di docker-compose)
AMQP_URL="amqp://rabbitmq:5672"